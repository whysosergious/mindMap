<div id="viewport">
  <div id="canvas" @click.passive="closeOpenWindows">
    <node-lines @conn-line="startDraw"></node-lines>
    <template v-for="(node, index) in nodes" :key="node.id">
      <step-node 
        :node="node"
        :cz="offset.canZ" 
        bg="blue"
        :hooks="nodeHooks"
        :initialNodeId="setInitialNode()"
        >
      </step-node>
    </template>
    <template v-for="(node, index) in webNodes">
      <web-canvas
        :node="node"
        :cz="offset.canZ"
        >
      </web-canvas>
    </template>
  </div>
  <interface
    :newMap="newMap"
    >
  </interface>
  <node-menu 
    :show="nodeMenu.show" 
    :x="nodeMenu.x" 
    :y="nodeMenu.y" 
    :addNode="addNode"
    :cz="offset.canZ"
    >
  </node-menu>
  <context-menu
    :show="contextMenu.show"
    :x="contextMenu.x" 
    :y="contextMenu.y" 
    :deleteNode="deleteNode"
    >    
  </context-menu>
</div>