import { _gc } from '/gc.js';

/** Context menu component */
export default {
  props: {
  },
  data() {
    return {
    }
  },
  template: await _gc.getTemplate('ContextMenu', true),
  methods: {
  },
  beforeMount() {
  },
  mounted() {
  }
}
